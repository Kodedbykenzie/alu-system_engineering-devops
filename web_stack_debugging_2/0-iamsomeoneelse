#!/usr/bin/env bash
# Check if exactly one argument is provided
if [ "$#" -ne 1 ]; then
    echo "Usage: $0 <username>"
    exit 1
fi

# Run the 'whoami' command as the specified user with an initialized environment
sudo -i -u "$1" whoami